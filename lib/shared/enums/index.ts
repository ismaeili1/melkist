export enum RecordStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  ARCHIVED = "ARCHIVED",
}

export enum SortDirection {
  ASC = "ASC",
  DESC = "DESC",
}

export enum YesNo {
  YES = "YES",
  NO = "NO",
}